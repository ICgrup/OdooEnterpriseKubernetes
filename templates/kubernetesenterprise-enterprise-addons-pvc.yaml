apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ printf "%s-enterprise-addons-pvc" .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.persistence.addonsSize | default "2Gi" }}
  storageClassName: {{ .Values.persistence.storageClass | default "nfs-client" }}